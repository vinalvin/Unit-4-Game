$(document).ready(function() {
    var wins = 0;
    var losses = 0;
    var random = [];

    function startGame() {
        var randomNum = Math.floor(Math.random() * 102) + 19;
        random.push(randomNum);
    }
})